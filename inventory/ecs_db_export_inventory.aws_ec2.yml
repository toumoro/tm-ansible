plugin: amazon.aws.aws_ec2
regions:
  - "{{ lookup('env', 'AWS_REGION') }}"
filters:
  instance-state-name: running 
  tag:Name: "{{ lookup('env', 'DUMP_INSTANCE_NAME') }}"
hostnames:
  - instance-id
compose:
  ansible_host: instance-id
